#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @n: The integer for which absolute value is computed.
 *
 * Return: The absolute value of n.
 */
int _abs(int n)
{
	return (n >= 0 ? n : -n);
}
