#include "main.h"

/**
 * main - prints a 1 if input is a letter, 0 otherwise
 *
 * Return: 0 (always success)
 */

int main(void)
{
	int _isalpha(int c);

	return (0);
}

int _isalpha(int c);
{
	int c;

	if (65 <= c <= 90 || 97 <= c <= 122)
	{
		return (1);
	}

	return (0);
}
