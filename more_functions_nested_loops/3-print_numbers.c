#include "main.h"

/**
 * print_numbers : prints the digits 0 through 9
 *
 * Return: Always 0.
 *
 */

void print_numbers(void)

{
	i = int

	for (i = 0; i <= 9; i++)
	{
		_putchar("%d\n", i);
	}
	return (0);
}
